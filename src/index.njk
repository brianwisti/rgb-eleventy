{% extends "layouts/base.njk" %}

{% from "macros/cards.njk" import note_card, post_card %}

{% block content %}
  <p>Yo, welcome!</p>

  <div>
    {% set note = collections.notes | first %}
    {{ note_card(note = note)}}
  </div>

  <div>
    {% for post in collections.posts | slice(5) | first %}
      {{ post_card(post = post) }}
    {% endfor %}
  </div>
{% endblock content %}
